# -----------------------------------------------------------------------
# Arbitrum Orbit Chain Deployment — Environment Variables
# -----------------------------------------------------------------------
# Copy this file to .env and fill in the values:
#   cp .env.example .env
#
# The three private keys below are REQUIRED. The rest have defaults.
#
# IMPORTANT: The parent chain must have Orbit contracts deployed.
# For local dev, fork Sepolia with Anvil:
#   anvil --fork-url https://sepolia.infura.io/v3/<YOUR_INFURA_KEY>
# -----------------------------------------------------------------------

# --- Required: Private keys (hex, with or without 0x prefix) -----------

# Chain deployer / owner (also used for contract deployment and funding)
DEPLOYER_PRIVATE_KEY=

# Batch poster
BATCH_POSTER_PRIVATE_KEY=

# Validator
VALIDATOR_PRIVATE_KEY=

# --- Chain parameters ---------------------------------------------------

# Parent chain RPC URL — Anvil forking Sepolia (default: http://localhost:8545)
# PARENT_CHAIN_RPC=http://localhost:8545

# L2 chain RPC URL (default: http://localhost:8449)
# L2_CHAIN_RPC=http://localhost:8449

# L2 chain ID (default: 97400766)
# CHAIN_ID=97400766

# Human-readable chain name (default: omega-messaging-chain)
# CHAIN_NAME=omega-messaging-chain

# --- Stylus Contract Deployment ----------------------------------------

# Private key for deploying Stylus contracts to L2 (defaults to DEPLOYER_PRIVATE_KEY)
# CONTRACT_DEPLOYER_KEY=

# --- Messaging Contract (set after deploy-contract.sh) ------------------

# Deployed Stylus messaging contract address
# MESSAGING_CONTRACT_ADDRESS=
# NEXT_PUBLIC_MESSAGING_CONTRACT=

# --- Load Testing -------------------------------------------------------

# Test user key for load tests (defaults to DEPLOYER_PRIVATE_KEY)
# TEST_USER_PRIVATE_KEY=

# Which load test to run (default: messaging)
# Options: messaging | burst | compute | all
# LOAD_TEST_MODE=messaging
